# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Mod Properties
mod_version=3.7.0
maven_group=com.example.jukeboxplus
archives_base_name=jukeboxplus

# Default Minecraft target. Override with -Pmc=<version>; every supported target
# has its own properties file in versions/<mc>.properties.
mc=1.21.11
